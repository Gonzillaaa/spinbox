# Redis service for test-multi-deps
# Generated by Spinbox on Fri Jul 18 11:32:52 CEST 2025

version: '3.8'

services:
  redis:
    image: redis:7
    container_name: test-multi-deps_redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
      - ./config/redis.conf:/usr/local/etc/redis/redis.conf:ro
    command: ["redis-server", "/usr/local/etc/redis/redis.conf"]
    networks:
      - app_network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

volumes:
  redis_data:
    driver: local

networks:
  app_network:
    driver: bridge
