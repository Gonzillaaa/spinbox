# {{PROJECT_NAME}}

A FastAPI backend project created with [Spinbox](https://github.com/Gonzillaaa/spinbox).

## Getting Started

1. Open this project in VS Code or Cursor
2. When prompted, click "Reopen in Container"
3. Wait for the DevContainer to build and start
4. Start database services (if any): `docker compose up -d`
5. Set up the Python environment: `./setup_venv.sh`
6. Start the server: `uvicorn app.main:app --reload --host 0.0.0.0`

## Project Structure

```
{{PROJECT_NAME}}/
├── .devcontainer/     # DevContainer configuration
├── app/               # FastAPI application
│   ├── main.py        # Application entry point
│   ├── core/          # Core configuration
│   │   ├── config.py  # Settings management
│   │   └── database.py # Database connection
│   ├── api/           # API routes
│   │   └── api_v1/    # API version 1
│   ├── models/        # SQLAlchemy models
│   └── schemas/       # Pydantic schemas
├── tests/             # Test files
├── .env.example       # Environment template
├── requirements.txt   # Python dependencies
├── Dockerfile         # Production container
└── Dockerfile.dev     # Development container
```

## Services

{{SERVICES_INFO}}

## Available Commands

```bash
# Start database services
docker compose up -d

# Start development server
uvicorn app.main:app --reload --host 0.0.0.0

# Run tests
pytest

# Run tests with coverage
pytest --cov=app

# Format code
black .

# Database migrations (if using Alembic)
alembic upgrade head
```

## API Documentation

When the server is running:
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

---
Generated by [Spinbox](https://github.com/Gonzillaaa/spinbox)
