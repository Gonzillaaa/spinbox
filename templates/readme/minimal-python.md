# {{PROJECT_NAME}}

A Python project created with [Spinbox](https://github.com/Gonzillaaa/spinbox).

## Getting Started

1. Open this project in VS Code or Cursor
2. When prompted, click "Reopen in Container"
3. Wait for the DevContainer to build and start

The virtual environment will be automatically activated when you open a terminal.

## Project Structure

```
{{PROJECT_NAME}}/
├── .devcontainer/     # DevContainer configuration
├── src/               # Source code
│   ├── __init__.py
│   └── core.py
├── tests/             # Test files
│   └── test_main.py
├── main.py            # Entry point
├── requirements.txt   # Python dependencies
└── pytest.ini         # Test configuration
```

## Available Commands

```bash
# Run the application
python main.py

# Run tests
pytest

# Run tests with coverage
pytest --cov=src

# Format code
black .

# Sort imports
isort .

# Install new packages
uv pip install <package-name>
```

## Development

- Source code goes in `src/`
- Tests go in `tests/`
- Add dependencies to `requirements.txt`

---
Generated by [Spinbox](https://github.com/Gonzillaaa/spinbox)
